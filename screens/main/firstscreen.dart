import 'package:flutter/material.dart';

class BottleScreen extends StatelessWidget {
  const BottleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text("bottle Spin")),
      ),
    );
  }
}
